# 🏗️ Arquitetura GastandoYa API - Com Vercel KV

## Diagrama de Arquitetura

```
┌─────────────────────────────────────────────────────────────────────┐
│                        App iOS (GastandoYa)                         │
│                                                                     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐             │
│  │   OAuth UI   │  │   Expenses   │  │  SwiftData   │             │
│  │  (Safari)    │  │   Viewer     │  │   Storage    │             │
│  └──────────────┘  └──────────────┘  └──────────────┘             │
└─────────────────────────────────────────────────────────────────────┘
                              ↓ HTTPS (x-api-key)
┌─────────────────────────────────────────────────────────────────────┐
│                    Backend Next.js (Vercel)                         │
│                                                                     │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │                    API Routes (App Router)                  │   │
│  │                                                             │   │
│  │  GET  /api/notion/oauth/authorize    ┌──────────────┐     │   │
│  │  GET  /api/notion/oauth/callback     │  OAuth Flow  │     │   │
│  │  POST /api/notion/config/database    └──────────────┘     │   │
│  │  GET  /api/notion/expenses           ┌──────────────┐     │   │
│  │                                       │ Data Access  │     │   │
│  │                                       └──────────────┘     │   │
│  └────────────────────────────────────────────────────────────┘   │
│                              ↓                                     │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │              Business Logic (lib/)                          │   │
│  │                                                             │   │
│  │  ┌──────────────────────────────────────────────────┐     │   │
│  │  │  NotionExpensesService                           │     │   │
│  │  │  - fetchExpensesForUser()                       │     │   │
│  │  │  - notionPageToExpenseDTO()                     │     │   │
│  │  └──────────────────────────────────────────────────┘     │   │
│  │                              ↓                             │   │
│  │  ┌──────────────────────────────────────────────────┐     │   │
│  │  │  UserNotionConnectionStore (Factory)             │     │   │
│  │  │                                                  │     │   │
│  │  │  if (KV_vars available)                         │     │   │
│  │  │     → VercelKVStore ✅                          │     │   │
│  │  │  else                                           │     │   │
│  │  │     → InMemoryStore (fallback)                 │     │   │
│  │  └──────────────────────────────────────────────────┘     │   │
│  └────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
                    ↓                              ↓
      ┌─────────────────────────┐    ┌─────────────────────────┐
      │   Vercel KV (Redis)     │    │   Notion API            │
      │                         │    │                         │
      │  ┌──────────────────┐   │    │  POST /v1/databases/   │
      │  │ notion:connection│   │    │       {id}/query       │
      │  │ :userId → data   │   │    │                         │
      │  └──────────────────┘   │    │  OAuth Token Exchange  │
      │                         │    │  Database Query        │
      │  Persistent Storage     │    │                         │
      │  Auto-Expiry: 90 days   │    └─────────────────────────┘
      └─────────────────────────┘
```

## Fluxo de Dados

### 1. OAuth Flow (Conectar Notion)

```
┌──────────┐                                    ┌──────────┐
│ App iOS  │                                    │  Notion  │
└────┬─────┘                                    └────┬─────┘
     │                                               │
     │ 1. GET /api/notion/oauth/authorize           │
     │    ?userId=user123                           │
     ├──────────────────────────────────────────────▶
     │                                               │
     │ 2. Redirect to Notion auth page              │
     │◀──────────────────────────────────────────────┤
     │                                               │
     │ 3. User authorizes                            │
     ├──────────────────────────────────────────────▶
     │                                               │
     │ 4. Callback: code + state                    │
     │◀──────────────────────────────────────────────┤
     │                                               │
     │ 5. Exchange code for token                   │
     ├──────────────────────────────────────────────▶
     │                                               │
     │ 6. Access token                              │
     │◀──────────────────────────────────────────────┤
     │                                               │
     │ 7. Save to Vercel KV                         │
     ├────────────────────────┐                     │
     │                        │                     │
     │ ┌────────────────────┐ │                     │
     │ │ KV: user123 →      │◀┘                     │
     │ │ {accessToken,      │                       │
     │ │  workspaceId, ...} │                       │
     │ └────────────────────┘                       │
     │                                               │
     │ 8. Success page                              │
     │◀──────────────────────────────────────────────┤
     │                                               │
```

### 2. Fetch Expenses

```
┌──────────┐                          ┌──────────┐
│ App iOS  │                          │  Backend │
└────┬─────┘                          └────┬─────┘
     │                                     │
     │ 1. GET /api/notion/expenses         │
     │    Headers: x-api-key, x-user-id    │
     ├────────────────────────────────────▶│
     │                                     │
     │                     2. Validate key │
     │                                     │
     │               3. Get from Vercel KV │
     │                     ┌───────────────┤
     │                     │               │
     │                     │ ┌───────────┐ │
     │                     └─│ KV: Get   │ │
     │                       │ user data │ │
     │                       └───────────┘ │
     │                                     │
     │           4. Query Notion Database  │
     │                     ┌───────────────┤
     │                     │               │
     │                     │ Notion API    │
     │                     │ /databases/   │
     │                     │ {id}/query    │
     │                     └───────────────┤
     │                                     │
     │           5. Transform to ExpenseDTO│
     │                                     │
     │ 6. Return JSON                      │
     │◀────────────────────────────────────┤
     │                                     │
     │ 7. Save to SwiftData                │
     ├──────────┐                          │
     │          │                          │
     │ ┌────────▼────────┐                 │
     │ │ SwiftData Store │                 │
     │ └─────────────────┘                 │
     │                                     │
```

## Componentes Principais

### 1. Storage Layer (Dual Implementation)

```typescript
// Factory (userNotionConnectionStore.ts)
┌─────────────────────────────────────┐
│ getUserNotionConnectionStore()      │
│                                     │
│ Detecta ambiente:                   │
│ - FORCE_IN_MEMORY_STORE=true?      │
│ - KV_REST_API_URL exists?          │
│ - Fallback to in-memory            │
└─────────────────────────────────────┘
              ↓          ↓
    ┌─────────────┐  ┌──────────────┐
    │   KV Store  │  │ Memory Store │
    │             │  │              │
    │ Persistent  │  │  Volatile    │
    │ Production  │  │  Dev/Backup  │
    └─────────────┘  └──────────────┘
```

### 2. Notion Service Layer

```typescript
NotionExpensesService
├── fetchExpensesForUser(userId)
│   ├── 1. Get connection from store
│   ├── 2. Create Notion client
│   ├── 3. Query database (with pagination)
│   └── 4. Transform to ExpenseDTO[]
│
└── notionPageToExpenseDTO(page)
    ├── Extract: description (title/rich_text)
    ├── Extract: date (date property)
    ├── Extract: amount (number → centavos)
    ├── Extract: category (select/rich_text)
    └── Return: ExpenseDTO
```

### 3. API Security

```
Request Flow:
┌────────────────────────────────────┐
│ Incoming Request                   │
└────────────────┬───────────────────┘
                 ↓
      ┌──────────────────────┐
      │ Validate x-api-key   │
      │ (APP_API_KEY)        │
      └──────────┬───────────┘
                 ↓
        ✅ Valid  │  ❌ Invalid
                 │      ↓
                 │   Return 401
                 ↓
      ┌──────────────────────┐
      │ Extract x-user-id    │
      └──────────┬───────────┘
                 ↓
      ┌──────────────────────┐
      │ Process Request      │
      └──────────────────────┘
```

## Performance

### Latency Estimada

```
OAuth Flow:
- Step 1 (authorize): ~50ms (redirect)
- Step 2 (user auth): ~user interaction~
- Step 3 (callback): ~200ms (token exchange + KV save)
- Total: ~250ms + user time

Fetch Expenses:
- Validate API key: <1ms
- Get from KV: ~5-10ms
- Query Notion: ~200-500ms (depends on database size)
- Transform data: ~10-50ms
- Total: ~220-560ms

Target: p95 < 800ms ✅
```

### Scalability

```
Vercel KV Free Tier:
├── Storage: 3 GB
│   └── ~6M connections possible
├── Memory: 256 MB
│   └── Active dataset always in memory
└── Commands: 30M/month
    └── ~10M requests/month

GastandoYa Usage per Request:
├── OAuth callback: 1 SET
├── Fetch expenses: 1 GET
└── Average: 2-3 commands/request

Capacity: Thousands of active users ✅
```

## Deployment Architecture

```
┌─────────────────────────────────────────────────────────┐
│                    Vercel Platform                      │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │            Serverless Functions                  │   │
│  │                                                  │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐      │   │
│  │  │ Instance │  │ Instance │  │ Instance │      │   │
│  │  │    1     │  │    2     │  │    N     │      │   │
│  │  └────┬─────┘  └────┬─────┘  └────┬─────┘      │   │
│  └───────┼─────────────┼─────────────┼────────────┘   │
│          │             │             │                 │
│          └─────────────┴─────────────┘                 │
│                        ↓                                │
│  ┌─────────────────────────────────────────────────┐   │
│  │            Vercel KV (Redis)                    │   │
│  │                                                  │   │
│  │  - Single source of truth                       │   │
│  │  - Shared across all instances                  │   │
│  │  - Auto-scaling                                 │   │
│  │  - High availability                            │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

## Segurança

### Camadas de Segurança

```
1. Transport Layer
   └── HTTPS obrigatório (TLS 1.3)

2. Authentication Layer
   └── x-api-key validation

3. Storage Layer
   └── Vercel KV (managed Redis)
       ├── Encrypted at rest
       ├── Encrypted in transit
       └── Isolated per account

4. External API Layer
   └── Notion OAuth tokens
       ├── Never exposed to client
       ├── Stored encrypted in KV
       └── Auto-expire (90 days)

5. Application Layer
   └── Input validation
   └── Error handling (no info leak)
```

---

**Versão:** 1.0 com Vercel KV  
**Última atualização:** Dezembro 2025  
**Status:** ✅ Produção-ready












